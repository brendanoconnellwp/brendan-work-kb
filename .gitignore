# Obsidian
.obsidian/workspace.json
.obsidian/workspace-mobile.json
.obsidian/cache
.obsidian/plugins/obsidian-git/data.json
.trash/

# OS
.DS_Store
Thumbs.db
desktop.ini

# Build artifacts
node_modules/

# Local-only leak-check script — contains real client/teammate names, never commit
leak-check.ps1


# Raw research corpus — lives locally only, never pushed.
# These are source materials (fetched articles, notes, videos).
# The compiled wiki/ articles are what gets committed.
raw/articles/*
raw/notes/*
raw/videos/*
raw/clippings/*
raw/images/*
raw/daily/*

# Keep folder structure and README files
!raw/**/README.md
!raw/**/.gitkeep
